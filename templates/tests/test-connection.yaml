apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "anime.fullname" . }}-test-connection"
  labels:
    {{- include "anime.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "anime.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
